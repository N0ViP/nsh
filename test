1.    echo hello && (ls | grep ".c") || echo "fallback" && ls | grep "a"

                  OR
            /           \
        AND                       AND
        /  \               /              \ 
    CMD:       |         CMD:            PIPE
     echo     PIPE        echo fallback  /   \
     hello    /   \                     ls   grep "a"
             ls   grep ".c"
           
           






2.  (ls -a)(pwd) 
    # (ls -a))pwd
    # (ls -a)(pwd



3.  (echo hi && ls) | grep foo

         PIPE
        /    \
    SUBSHELL   grep foo
        |
    AND
    /   \
    echo  ls


4.   a="yoo"&&(a="hi"&&echo $a)&&echo $a

5.  echo hi && (ls || (pwd && whoami))

6.  ((ls | cat)&& ls) //wtf ((ls | cat)<<end)

7.  echo 'hi' | echo "change this "$SHELL | grep f | wc -l > $USER | cat < input.txt >output.txt | ls >> file.log -a | echo "im in here =>$HOME" '$PATH'$USERP $? $9po